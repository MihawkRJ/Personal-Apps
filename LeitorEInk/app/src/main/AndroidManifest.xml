<?xml version="1.0" encoding="utf-8"?>
<!-- Leitor E-Ink · Copyright (c) 2026 Widney Silva · Licença MIT -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Necessário apenas em Android antigos (<= 9). No Android 10+ usamos
         Storage Access Framework (SAF), que não exige essas permissões. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".EInkReaderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.LeitorEInk"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LeitorEInk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.ReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.LeitorEInk.Reader"
            android:configChanges="orientation|screenSize|keyboardHidden" />

    </application>
</manifest>
